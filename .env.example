# Frontend (Vite). Leave unset to run in local demo mode (localStorage, no server).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only — used by scripts/import-bible.mjs. NEVER expose to the frontend.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
